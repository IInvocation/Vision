<controls:MetroWindow x:Class="FluiTec.Vision.Client.Windows.EndpointManager.Views.SetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="{Binding Title}" 
		Height="300" 
		Width="500" 
		DataContext="{Binding Setup, Source={StaticResource Locator}}"
		BorderThickness="0" 
		GlowBrush="Black">
	<Grid>
		<xctk:Wizard FinishButtonClosesWindow="False" 
					 HelpButtonVisibility="Hidden" 
					 CancelButtonContent="Abbrechen" 
					 BackButtonContent="Zurück" 
					 NextButtonContent="Weiter" 
					 FinishButtonContent="Abschließen" 
					 ItemsSource="{Binding Wizard.XPages}" 
					 CurrentPage="{Binding Wizard.CurrentXPage}" 
					 CanSelectNextPage="{Binding Wizard.CanSelectNextPage}" 
					 CanFinish="{Binding Wizard.CanFinish}">
		</xctk:Wizard>
    </Grid>
</controls:MetroWindow>
