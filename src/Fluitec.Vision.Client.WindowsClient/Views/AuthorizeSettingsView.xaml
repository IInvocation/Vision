<Window x:Class="Fluitec.Vision.Client.WindowsClient.Views.AuthorizeSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Autorisierungs-Einstellungen" Height="300" Width="300" DataContext="{Binding Source={StaticResource Locator}, Path=AuthorizeSettings}">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10,0,10">
			<Label Width="100">EMail:</Label>
			<TextBox Width="150" Text="{Binding Email, Mode=OneWay}" />
		</StackPanel>
	    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
		    <Label Width="100">Computername:</Label>
		    <TextBox Width="150" Text="{Binding MachineName, Mode=OneWay}" />
	    </StackPanel>
		<StackPanel Grid.Row="2" Margin="0,10,0,10">
			<Button>Aktivierung anfordern</Button>
			<TextBox Text="{Binding ActivationCode}"></TextBox>
			<Button>Aktivierung abschließen</Button>
		</StackPanel>
    </Grid>
</Window>
